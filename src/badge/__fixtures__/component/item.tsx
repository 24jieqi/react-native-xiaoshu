import React from 'react'
import { View } from 'react-native'

const style = {
  width: 40,
  height: 40,
  backgroundColor: '#000',
  borderRadius: 4,
}

const BadgeDemoItem: React.FC = () => {
  return <View style={style} />
}

export default BadgeDemoItem
